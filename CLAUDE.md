# 산단 심박

경상북도 산업도시의 월별 전력 사용량을 심박으로 시각화하는 **정적 웹**.
2006.01~2025.06 시계열을 슬라이더로 재생하고, 지역을 클릭하면 2.5D 상세 화면으로 들어간다.

## 실행

```bash
python3 -m http.server 8000    # → http://localhost:8000
```

빌드 단계 없음. node/npm 사용하지 않는다. 브라우저가 `<script type="module">`로
소스를 직접 읽는다.

## 폴더 구조

```
data/raw/         한전 원본 xlsx. 절대 수정하지 않는다.
data/processed/   스크립트가 생성한 JSON. 손으로 고치지 않는다.
data/geo/         경계 원본 (GeoJSON)
scripts/          raw + geo → processed 변환 (python)
src/              프론트엔드 (vanilla JS, ES modules)
index.html        엔트리
```

## 데이터 스키마

`data/processed/frames.json` — 이 모양을 바꾸지 말 것.

```json
{
  "months": ["2006-01", "2006-02", "...", "2025-06"],
  "regions": {
    "gumi": {
      "name": "구미시",
      "path": "M120,340 L128,338 ...",
      "cx": 180,
      "cy": 300,
      "hasData": true,
      "frames": [
        { "rate": 1.02, "size": 0.81, "irr": 0.12 }
      ]
    }
  }
}
```

| 필드 | 의미 |
|---|---|
| `path` | SVG path 문자열. **파이썬에서 미리 투영된 픽셀 좌표** |
| `cx`, `cy` | 심박 점 위치 (픽셀) |
| `hasData` | `false`면 회색 처리, 심박 없음, `frames`는 빈 배열 |
| `rate` | 최근 12개월 합 ÷ 직전 12개월 합 → 깜빡임 주기 |
| `size` | 전력 총량 (0~1 정규화) → 점 크기 |
| `irr` | 계절 패턴 편차 → 깜빡임 흔들림 |

`frames` 배열 길이 = `months` 배열 길이. 인덱스가 서로 대응한다.

SVG 좌표계는 **900 × 700 고정**. 반응형은 `viewBox`로 처리한다.

## 규칙

- **좌표 투영은 파이썬에서 미리 한다.** 브라우저에 지도 라이브러리(d3-geo, leaflet, mapbox 등)를 쓰지 않는다.
- **`frames`는 전부 사전 계산한다.** 런타임에 시계열을 집계하지 않는다.
- **카메라 자유 회전 / 팬 / 줌을 넣지 않는다.** 2.5D는 고정 각도 + fit-to-bounds. 필요하면 프리셋 뷰 버튼.
- **`data/raw/`는 읽기 전용.** 모든 변형은 `scripts/` 안에서 한다.
- 2.5D는 **CSS 3D transform**으로 구현한다 (Three.js 사용 금지).
- **클러스터 위기도는 `health` 값만 읽는다.** 산출식을 프론트에 구현하지 않는다.
- 요청하지 않은 기능을 추가하지 않는다.
- 스타일링은 기능이 동작한 뒤에 한다.

## 시각화 대상 (2026-08 방향 변경)

**주인공은 산업 클러스터다. 시군구가 아니다.**

전력은 시군구 × 월별 집계라 개별 기업의 위험을 볼 수 없다. 그래서 전력을
주지표에서 내리고 **보조 입력 하나**로 강등한다.

| 층 | 역할 | 단위 |
|---|---|---|
| **산업 클러스터** | **주인공.** 위기도를 점으로 표현 | 연계 기업 묶음 |
| 시군구 지역 색 | 배경 맥락 (전력 기반) | 시군구 × 월 |

클러스터의 **위기도(health)** 는 굴뚝 배출·전력·매출 등을 종합해 산출한다.
**산출식은 팀메이트가 별도로 설계 중이다.** 이 저장소의 프론트엔드는
계산식을 알지 않고 `health` 값만 읽는다 — 아래 계약을 지키면 산출식이
바뀌어도 프론트는 수정하지 않는다.

### 계약: `data/processed/clusters.json`

```json
{
  "months": ["2006-01", "...", "2025-06"],
  "clusters": {
    "gumi-1": {
      "name": "구미1산단",
      "region": "gumi",
      "cx": 350.2, "cy": 403.1,
      "members": 18,
      "frames": [
        { "health": 0.72, "inputs": { "power": 0.81, "stack": 0.64, "revenue": 0.70 } }
      ]
    }
  }
}
```

| 필드 | 계약 |
|---|---|
| `health` | **0~1. 낮을수록 위기.** 화면이 쓰는 유일한 값 |
| `inputs` | 선택. 툴팁·분해 표시용. 없어도 화면은 동작해야 한다 |
| `cx`, `cy` | 900×700 좌표계 (`frames.json` 과 동일) |
| `frames` | 길이 = `months` 길이 |

**프론트엔드 규칙: `health` 외의 값으로 시각적 상태를 결정하지 않는다.**
`inputs` 의 키 구성은 언제든 바뀔 수 있다.

## 현재 데이터 상태

| 파일 | 상태 |
|---|---|
| `frames.json` 의 `rate`/`size`/`irr` | **난수.** 한전 실데이터 미확보 |
| `buildings.mock.json` | **가상 데이터.** 팩토리온 실데이터 확보 후 삭제 |
| `clusters.mock.json` 의 `health` | **난수.** 완만한 하락 + 계절성 + 일부 급락 |
| 가상 데이터의 범위 | **안동시 하나뿐.** 건물 110채 / 클러스터 6개 |
| `clusters.json` | **아직 없음.** 팀메이트 산출물 대기 |

`data/processed/buildings.mock.json` 은 가상 데이터다.
팩토리온 실데이터 확보 후 삭제할 것. (생성: `scripts/build_mock_buildings.py`,
렌더링: `src/detail.js` 의 `renderBuildings()`, 배지: `#mock-badge`)

`data/processed/clusters.mock.json` 도 가상 데이터다. 계약(위 `clusters.json`)은
그대로 지키고 `health` 만 난수로 채운 임시 파일이다 — **산출식과 무관하다.**
팀메이트의 `clusters.json` 이 오면 스크립트와 함께 삭제한다.
(생성: `scripts/build_mock_clusters.py` ← `buildings.mock.json` 의 `clusters`,
렌더링: `src/main.js` 의 `healthToFill()` / `#clusters`,
배지: `index.html` 의 `#mock-badge`)

가상 데이터는 **안동시에만** 만든다 (`build_mock_buildings.py` 의 `MOCK_REGIONS`).
22개 시군구 전부에 깔면 파일이 열 배 이상 커지고 전체 지도가 점으로 뒤덮여,
느낌을 보는 목적에도 방해가 된다. 다른 지역의 detail 화면은 땅만 나오고
배지도 뜨지 않는다 — 정상이다.

건물 무리 = 클러스터다. `build_mock_buildings.py` 가 격자로 뭉쳐 놓은 무리를
`clusters` 로 요약해(무게중심·개수·바닥면적) 같은 파일에 넣고, 각 건물은
`clusterId` 를 들고 있다. 두 파일의 클러스터 좌표는 같은 자리를 가리킨다.

## 데이터 출처

- 전력: 한국전력공사 전력데이터 개방포털 — 아래 "실데이터 경로" 참고
- 경계: `vuski/admdongkor` ver20260701 행정동 GeoJSON (경북 필터 + dissolve)
- 공장: 전국산업단지및공장정보표준데이터 (팩토리온)

## 실데이터 경로 (확인 완료)

**`업종별 전력사용량`을 쓴다. `산업분류별`은 쓰지 않는다.**

| | 산업분류별 (pcode=000167) | 업종별 (pcode=000504) |
|---|---|---|
| 분류 | KSIC 대분류 21개 | 한전 자체 업종 38개 |
| 제조업 | **"제조업" 하나뿐** | **1차금속 / 영상·음향 등으로 분리** |
| 사용 | ❌ | ✅ |

조회: `https://bigdata.kepco.co.kr/cmsmain.do?scode=S01&pcode=000504&pstate=L&redirect=Y`

행정구역 전국 / 시도 / **시·군·구** 단계 선택, 월 단위, 2007.01~2025.06.
전전월 통계치를 매월 16일경 갱신. 조회는 되고 **다운로드는 로그인 필요**.

도시별로 쓸 업종:

| 도시 | 업종 |
|---|---|
| 포항 | **1차금속** |
| 구미 | **영상·음향** (보조: 전기기기) |

업종 전체 38개: 농업·임업, 어업, 석탄·원유, 금속비금속, 식료품제조, 음료품제조,
담배제조업, 섬유, 의복·모피, 가죽·신발, 목재·나무, 펄프·종이, 출판·인쇄, 석유정제,
화학제품, 고무·플라, 유리, 시멘트, 1차금속, 조립금속, 기타기계, 사무기기, 전기기기,
영상·음향, 의료·광학, 자동차, 기타운송, 가구및기타, 재생재료, 수도, 사업자용, 전철,
가정용부문, 국군용, 유엔군용, 기타공공용, 관공용, 순수써비스

⚠️ KSIC가 아니라 한전 자체 분류다. 발표에서 "전자부품"이라 하지 말고
**"영상·음향"** 또는 "전자"로 표현한다.

## 미해결

- 포항 2022년 9월(힌남노) 1차금속 전력 급락이 실제로 관측되는지.
  포스코 자가발전 비중 때문에 안 보일 수 있다. **데이터 받으면 이 셀부터 확인.**
- 조회 UI의 1회 최대 추출 기간 (234개월을 한 번에 받을 수 있는지)
- `산업분류별 읍/면/동 자료` (pcode=000502) 의 분류 수준 — 미확인

## 렌더링 주의 (실제로 걸렸던 것)

- `<!doctype html>` 을 빼면 쿼크 모드로 렌더되어 **SVG 크기가 0×0** 이 된다.
  반드시 doctype을 넣고, SVG에는 `aspect-ratio: 900/700` 을 준다.
- 시군구 경계는 행정동을 `dissolve()` 로 병합해 서브패스 1개로 만든다.
  병합하지 않으면 행정동 내부 경계선이 hairline 으로 드러난다.
- 경북에는 **군위군 자리에 구멍**이 있다 (2023.7 대구 편입). 정상이다.

## 정확도

투영 스케일 1px ≈ 278m. 단순화 허용오차 0.7px = **실제 약 195m**.
정점 41,007 → 3,346개(-91.8%). 도 단위 시군구 형태 식별에는 충분하지만
필지·건물 단위 판정에는 부적합하다. 공장의 소속 시군구는 좌표 판정이 아니라
**팩토리온 데이터의 주소 필드**로 정한다.
